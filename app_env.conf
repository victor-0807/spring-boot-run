#java程序路径（必需）
JAVA_EXE=/usr/bin/java

#注册中心地址（如果没用注册中心，该项可注释掉）
#MSP_DISCOVERY_URI=http://ms:ms_1234@10.34.110.160:18761/eureka/

#配置文件选择
PROFILES_ACTIVE=single

#服务端口（必需）
SERVER_PORT=18761

#JVM配置（必需）
JAVA_OPTIONS=("-Xms256m -Xmx1024m" "-XX:PermSize128m" "-Djava.io.tmpdir=/var/tmp" "-Duser.timezone=Asia/Shanghai")

#Spring Boot属性配置(如果没有，可以留空)
SPRING_BOOT_OPTIONS=""
